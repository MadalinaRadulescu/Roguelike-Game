
def display_board(board, player):
    # hedears = "#"*(width+2)
    # first_row_last = ""
    # for i in hedears:
    #     first_row_last += " ".join(i)
    # print(first_row_last)
    # for x in board:
    #     print("#" + "".join(x) + "#")
    # print(first_row_last)
    for i in board:
        print("".join(i))
    

        
    
